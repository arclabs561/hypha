/target
target/
.cursor/
.vscode/
.idea/
/.cargo/config.toml
.embuild/
/Cargo.lock
# build artifacts and signing keys never ship in the public repo: a built image
# bakes WiFi + MQTT credentials, and priv.pem is the mesh-OTA signing key
firmware/*/build/
firmware/mesh_ota/keys/
